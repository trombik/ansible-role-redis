---
- hosts: localhost
  pre_tasks:
  roles:
    - ansible-role-redis
  vars:
    redis_config:
      databases: 17
      save 900: 1
      requirepass: "{{ redis_password }}"
    redis_password: password
